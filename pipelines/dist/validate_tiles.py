def validates_tiles(tiles_uri: str) -> bool:
    """Validate DIST tiles on Data API to confirm there's no issues with the input data."""
    # check coverage
    # checking total global stats (sum, avg, etc.)
    pass
