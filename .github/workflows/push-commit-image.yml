name: Build and Push Docker Image to AWS ECR

on:
  push:

jobs:
  build-and-push:
    runs-on: ubuntu-24.04-arm
    container: "ghcr.io/osgeo/gdal:ubuntu-small-3.10.3"
    steps:
      # Checkout the repository
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install sudo, node for later actions
        if: success()
        shell: bash
        run: |
          apt-get update
          apt-get install sudo nodejs -y

#      - name: Install CLANG for building rasterio deps
#        if: success()
#        id: install_clang
#        uses: rlalik/setup-cpp-compiler@master
#        with:
#          compiler: clang-latest
#
#      - name: Install GCC for building rasterio deps
#        if: success()
#        id: install_gcc
#        uses: rlalik/setup-cpp-compiler@master
#        with:
#          compiler: gcc-latest
#
#      - name: Install uv, create a VENV, and enable caching
#        if: success()
#        uses: astral-sh/setup-uv@v6
#        with:
#          enable-cache: true
#          activate-environment: true
#          prune-cache: false
#
#      - name: Install project dependencies
#        if: success()
#        shell: bash
#        env:
#          CC: ${{ steps.install_clang.outputs.cc }}
#          GCC: ${{ steps.install_gcc.outputs.cc }}
#          MAKEFLAGS: "-j$(nproc)"
#        run: |
#          cd $GITHUB_WORKSPACE
#          uv sync --locked --no-install-project --dev -p 3.13.5
#
#      - name: Run tests
#        if: success()
#        shell: bash
#        run: |
#          cd $GITHUB_WORKSPACE
#          pytest test --maxfail=1 --disable-warnings

      - name: Configure AWS credentials
        if: success()
        uses: aws-actions/configure-aws-credentials@v4
        env:
          AWS_REGION: us-east-1
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}  # Store AWS credentials in GitHub Secrets
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1  # ${{ secrets.AWS_REGION }}

      - name: Set up Docker
        if: success()
        uses: docker/setup-docker-action@v4

      - name: Login to Amazon ECR Private
        if: success()
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
#        env:
#          AWS_DEFAULT_REGION:
#          AWS_REGION:
#          AWS_ACCESS_KEY_ID:
#          AWS_SECRET_ACCESS_KEY:
#          AWS_SESSION_TOKEN:

#      - name: Test step
#        run: |
#          echo $AWS_SESSION_TOKEN

#      - name: Build Docker Image
#        run: |
#          IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)  # Use the first 7 characters of the commit hash as the tag
#          docker build -t $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$REPOSITORY_NAME:$IMAGE_TAG .
#          echo Image tag: $IMAGE_TAG

#      # Push the Docker image to ECR
#      - name: Push Docker Image to ECR
#        run: |
#          IMAGE_TAG=$(echo $GITHUB_SHA | cut -c1-7)
#          docker push $AWS_ACCOUNT_ID.dkr.ecr.$AWS_REGION.amazonaws.com/$REPOSITORY_NAME:$IMAGE_TAG

#env:
#  AWS_REGION: us-east-1  # Replace with your AWS region
#  REPOSITORY_NAME: zeno-fastapi-app  # Replace with your ECR repository name
#      AWS_ACCOUNT_ID: ${{ secrets.AWS_KEY_PRODUCTION }}  # Store your AWS account ID in GitHub Secrets
#      AWS_ACCOUNT_ID: ${{ secrets.AWS_SECRET_PRODUCTION }}  # Store your AWS account ID in GitHub Secrets
